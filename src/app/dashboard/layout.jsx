import React from 'react';

const DashBoardLayOut = ({children}) => {
    return (
        <div>  
            hi
            <main>
                {children}
            </main>
        </div>
    );
};

export default DashBoardLayOut;